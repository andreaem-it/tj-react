---
description: Eseguire commit e push dopo modifiche al codice
alwaysApply: true
---

# Git: commit e push dopo le modifiche

Dopo aver modificato file del progetto (codice, config, documentazione):

1. **Verifica** lo stato: `git status`
2. **Aggiungi** solo i file rilevanti (escludi `.env.local` e file sensibili): `git add <file>...` o `git add -A` se appropriato
3. **Commit** con messaggio chiaro e conciso (preferibilmente convenzione conventional commits: `tipo(scope): messaggio`)

## Frontend (repo `tj-react`, remote `origin`)

- Push sul **branch di lavoro** indicato dall’utente (es. feature), **mai** `git push origin main` senza **richiesta esplicita** (la `main` è il frontend in produzione).
- `git branch --show-current` prima del push.

## Admin (app in `admin/`, deploy su **tj-react-admin**)

Il progetto Vercel dell’admin è collegato al repo **`https://github.com/andreaem-it/tj-react-admin`**, branch **`main`** (non usare `origin/feature/admin-d1` su `tj-react` per quel deploy).

Dopo commit che toccano `admin/` nel monorepo:

```bash
cd /path/to/techjournal-clone   # root monorepo, branch che contiene i commit aggiornati su admin/
git subtree split --prefix=admin -b tmp-admin-split
git push tj-react-admin tmp-admin-split:main
git branch -D tmp-admin-split
```

Remote `tj-react-admin` deve esistere (`git remote -v`). Se il push non è fast-forward, valutare `--force-with-lease` solo se consapevoli di sovrascrivere `main` del repo admin.

Non fare commit di:
- `.env.local` o file con segreti
- `node_modules/`
- file generati o cache (`.next/`, ecc.)

Se l'utente chiede esplicitamente di non fare commit/push, rispetta la richiesta.
